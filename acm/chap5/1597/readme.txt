这题相当的奇怪
踩了两个坑，第一个是输出找不到那里
还有一个是
map<string,set<int> > m;
m["aaa"];
m.count("aaa");  // 结果是1
所以我后来用的m["aaa"].size()

还有一个未解决的是超时问题，然而在我电脑上跑的比答案还快，但是交上去就是超了3s了，另外也在data.in里测试了最大数据集，只有1s不到，不知道为什么
两个答案的时间复杂度应该是一样的，M*N次循环（文章数*查询命令数）。
