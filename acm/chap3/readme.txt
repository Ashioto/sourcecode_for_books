UVa1585  Score
统计XOOXXOOXX字符串的得分值

设置一个变量反映当前O的权重即可，无坑


UVa1368  DNA Consensus String
算最小字典序的DNA字符串，以及Hamming距离

统计TGCA的个数，选择最多出现次数的那个。
最小字典序由TGCA顺序保证，如果出现多解的情况的话。


UVa455  Periodic String
*大坑*
计算周期串
abca类型
1、首先检查字符串长度是不是步长的倍数，排除了上面的坑爹情况；
2、从0到步长减一，对每个字符都加上布长进行判断，如果有不行的，flag置0；
3、最后一组数据没有\n。


UVa202  Repeating Decimals
*大坑（第二遍的时候没有初始化）+一开始还不会做*
计算循环小数的循环部分，使用了余数的想法
比较复杂，建议再看看
使用了几个数组来保存下标和该下标中的内容
技巧：使用了while来让余数再次出现时停下
余数为0和余数不为0单独考虑
计算循环小数开始和结束的部分有一些技巧


UVa227  Puzzle
*大坑（字符串读取函数）*
一个迷宫，里面有个空格，然后输入一些上下左右的指令，
让输出最后的迷宫形状，如果出界了的话输出报错（命令无效也是）

先读地图里的一行，判断第一个字符是不是‘Z’，如果不是的话再读下面的几行地图。
因为里面有空格，所以用scanf是不行的，而scanf读取一行的时候不会把\n包括进来，
但是\n依然还存在那里。如果接下来还是用的scanf的话，没事，如果用fgets的话，
fgets里的内容就只剩\n了，导致一开始地图的构建老是出错。
地图移动那边的逻辑想清楚，有的地方是不可以用单目运算符的。
还有一个就是case里的default了，如果命令无效，直接flag=0.


UVa232  Crossword Answer
填字游戏，让输出横竖两种看法下的有效的字符串

主要是\n那里比较麻烦，要比对清楚才行
另外竖着的时候并不是横着的时候的转置矩阵，遍历方向相同但是找词方向不同。
while循环很好用


UVa1587  Box
*小坑*
算6个矩形能不能拼成盒子

先统计独立的数字个数，不能超过3
1好办
2麻烦点，主要是搞清楚8个和4个到底是怎么组合的
3比较好办，两步，第一个看有没有长和宽相同的，有就不行
然后看是不是每个都是4个，不是也不行


UVa1588  Kickdown
换低低档装置

一个序列移位的问题，需要搞清楚偏移量正负时的影响。

					2016年1月27日 美赛前一天开始看的刘汝佳
